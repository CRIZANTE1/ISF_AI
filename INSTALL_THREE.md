# Instalação do Three.js

Para usar o componente DottedSurface na tela de login, você precisa instalar o Three.js:

```bash
npm install three @types/three
```

Ou se estiver usando yarn:

```bash
yarn add three @types/three
```

Após a instalação, o componente DottedSurface funcionará automaticamente na tela de login.

